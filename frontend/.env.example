# Local development can point directly at a running FastSell API.
# For the production nginx build, leave VITE_API_BASE_URL unset so the app uses same-origin /api.
VITE_API_BASE_URL=http://localhost:8888
